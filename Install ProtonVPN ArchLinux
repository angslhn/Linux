// Install ProtonVPN

// Install WireGuard Tools
// Ini untuk menyediakan wg dan wg-quick (alat utama WireGuard).
- sudo pacman -S wireguard-tools

// Install systemd-resolvconf
// Agar DNS dari ProtonVPN bisa dipasang lewat systemd-resolved
- sudo pacman -S systemd-resolvconf

// Pilih Server dan Download Configurasi WireGuard Milik ProtonVPN

// Pindahkan Config dari ProtonVPN ke WireGuard
- sudo mv <nama-file-configurasi>.conf /etc/wireguard/wg0.conf

// Menghidupkan Service DNS Bawaan systemd
- sudo systemctl start systemd-resolved

// Jalankan WireGuard
- sudo wg-quick up wg0

// Cek Status
- sudo wg

// Menghentikan WireGuard
- sudo wg-quick down wg0
